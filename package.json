{
  "name": "setupcachingtest",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha test --recursive",
    "test-docker": "docker-compose -f docker-compose.test.yml up && docker-compose -f docker-compose.test.yml down",
    "docker-mocha": "node ./docker-mocha/docker-mocha-run.js -f ./test/tests.json -t 1 -d docker-compose.yml",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "better-queue": "^3.8.10",
    "express": "^4.16.4",
    "js-yaml": "^3.12.2",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "mocha": "^6.0.2"
  },
  "description": ""
}
