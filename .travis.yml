language: node_js

sudo: true

node_js:
  - "11.12.0"

services:
  - docker

cache:
  directories:
    - node_modules

install:
  - npm install

script:
  - npm install

after_success:
  - npm run coverage
  - npm run report-coverage

